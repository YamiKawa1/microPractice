//  annadir domicilio, nacionalidad, editor de codigo favorito. 
// si la edad es mayor que 18 annos cambiar los colores en personal colors
// un log por cada editor de texto
function PersonalData(info) {
    return info
}

function PersonalColor(color) {
    return color
}

module.exports = {
    PersonalData,
    PersonalColor
}